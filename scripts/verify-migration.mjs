#!/usr/bin/env node
/**
 * Verificação pós-migração: HEAD em URLs antigas do WP → confere 301 + 200.
 *
 * **Stub Fase 1** — implementação em `docs/WP_IMPORT_SPEC.md` (seção "Verificação pós-import").
 */
console.error("[verify-migration] stub Fase 1 — não implementado. Ver docs/WP_IMPORT_SPEC.md.");
process.exit(1);
